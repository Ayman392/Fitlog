import React from 'react';
import PlanList from '../components/PlanList/PlanList';

const myPlan = () => {
    return (
        <div>
            <PlanList/>
        </div>
    );
};

export default myPlan;